name: CI Lint Check

on: [push]

jobs:
  lint-check:
    runs-on: ubuntu-latest
    steps:
      - name: composer lint
        uses: php-actions/composer@v1
        with:
          command: composer lint
          run: 
        working-directory: ./src

  